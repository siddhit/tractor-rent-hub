export const WA_NUMBER = '919723000299'; // Makim — the only number, confirmed 18 Sep 2026

// Cash only, settled directly with the team, outside the site. The 50/50
// split shown on the booking page is informational only — the site takes
// no payment and records none.
export const PAYMENT_METHOD = 'cash';
export const ARRIVAL_PCT = 0.5;

export const SITE_URL = 'https://farmulya.in';

// TODO: GA4 Measurement ID — analytics stay off until this is set
export const GA_ID = 'G-XXXXXXXXXX';
